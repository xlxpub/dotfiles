⏺ 所有工具已就绪，全面总结一下：

  ✅ 环境检查

  ┌───────────┬─────────┬──────┐
  │   工具    │  版本   │ 状态 │
  ├───────────┼─────────┼──────┤
  │ Neovim    │ v0.12.1 │ ✅   │
  ├───────────┼─────────┼──────┤
  │ gopls     │ v0.21.1 │ ✅   │
  ├───────────┼─────────┼──────┤
  │ goimports │ 已安装  │ ✅   │
  ├───────────┼─────────┼──────┤
  │ gofumpt   │ v0.9.2  │ ✅   │
  ├───────────┼─────────┼──────┤
  │ ripgrep   │ 15.1.0  │ ✅   │
  ├───────────┼─────────┼──────┤
  │ fd        │ 10.4.2  │ ✅   │
  └───────────┴─────────┴──────┘

  ✅ 配置文件

  ~/.config/nvim/
  ├── init.lua
  └── lua/
      ├── options.lua
      ├── keymaps.lua
      ├── lazy-bootstrap.lua
      └── plugins/
          ├── ui.lua          # 主题/文件树/状态栏/标签页
          ├── editor.lua      # 注释/括号/Git/surround/flash
          ├── telescope.lua   # 模糊搜索
          ├── treesitter.lua  # 语法高亮
          └── lsp.lua         # gopls + nvim-cmp + conform

  ---
  现在可以启动 Neovim 了：

  nvim

  首次启动会自动下载并安装所有插件（约 1~3 分钟）。安装完成后执行：

  :Lazy        # 查看插件安装状态
  :Mason       # 确认 gopls/gofumpt 等工具状态

  安装完插件后打开任意 .go 文件，即可使用：
  - K → 查看文档
  - gd → 跳转定义
  - <leader>ca → Code Action（自动补全 import 等）
  - 保存时自动用 goimports + gofumpt 格式化